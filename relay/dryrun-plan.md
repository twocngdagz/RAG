# Relay dry run — proves the loop, touches nothing real

### Batch 1

Create a file `relay/DRYRUN.txt` containing exactly one line:
`relay dry run — safe to delete`. Commit it locally with the message
`chore(relay): dry run`. Do nothing else — no other files, no pushes.
